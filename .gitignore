node_modules/
dist/
build/
.expo/
target/
test-ledger/
.env
.env.*
!.env.example
*.log
.DS_Store
**/android/app/build/
**/*.keystore
!debug.keystore
.anchor/
.yarn/
.surfpool/

# Keypairs. target/ is already ignored, but that is incidental — this is the
# rule. Never commit a keypair, on any cluster, however throwaway it feels.
*keypair*.json
id.json
/keys/
*.pem
