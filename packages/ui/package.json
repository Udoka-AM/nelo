{
  "name": "@nelo/ui",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "description": "Nelo's design system: one palette, one type scale, touch targets a thumb can hit, and the few components every screen is built from.",
  "scripts": {
    "test": "node --test \"test/*.test.ts\""
  },
  "files": [
    "src"
  ]
}
