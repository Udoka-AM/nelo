{
  "name": "@nelo/issue",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "description": "Issuing vouchers on the payer's phone, without ever signing one sequence over two messages.",
  "dependencies": {
    "@nelo/voucher": "workspace:*"
  },
  "devDependencies": {
    "@noble/curves": "^1.6.0",
    "@nelo/accept": "workspace:*"
  },
  "scripts": {
    "test": "node --test \"test/*.test.ts\""
  },
  "files": [
    "src"
  ]
}
